import { useQuery } from '@tanstack/react-query';
import { ShieldAlert, CheckCircle2, XCircle } from 'lucide-react';
import { Card } from '../components/ui/card';
import { api } from '../lib/api';

const dateTime = new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short' });

function eventLabel(eventType: string): string {
  return eventType.replace(/([a-z])([A-Z])/g, '$1 $2');
}

export function AuditLogPage() {
  const auditQuery = useQuery({ queryKey: ['securityAuditLogs'], queryFn: api.listAuditLogs });

  return <div className="space-y-6"><div><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-sky-500/10 text-sky-600"><ShieldAlert className="h-5 w-5" /></div><div><h2 className="text-xl font-semibold text-slate-950 dark:text-white">Audit Log</h2><p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Security and user-management activity visible to senior administrators.</p></div></div></div><Card className="overflow-hidden"><div className="overflow-x-auto"><table className="min-w-[1050px] w-full text-sm"><thead className="bg-slate-50/80 dark:bg-white/5"><tr className="border-b text-left"><th className="px-5 py-3 text-xs font-semibold uppercase tracking-wide text-slate-500">Time</th><th className="px-5 py-3 text-xs font-semibold uppercase tracking-wide text-slate-500">Event</th><th className="px-5 py-3 text-xs font-semibold uppercase tracking-wide text-slate-500">User</th><th className="px-5 py-3 text-xs font-semibold uppercase tracking-wide text-slate-500">IP address</th><th className="px-5 py-3 text-xs font-semibold uppercase tracking-wide text-slate-500">Details</th><th className="px-5 py-3 text-xs font-semibold uppercase tracking-wide text-slate-500">Result</th></tr></thead><tbody>{(auditQuery.data ?? []).map((entry) => <tr key={entry.id} className="border-b last:border-0 hover:bg-slate-50/60 dark:border-white/10 dark:hover:bg-white/[0.03]"><td className="whitespace-nowrap px-5 py-4 text-slate-500">{dateTime.format(new Date(entry.createdAt))}</td><td className="px-5 py-4 font-medium text-slate-800 dark:text-slate-100">{eventLabel(entry.eventType)}</td><td className="px-5 py-4"><div>{entry.email ?? 'Unknown account'}</div>{entry.userAgent && <div className="mt-1 max-w-xs truncate text-xs text-slate-400" title={entry.userAgent}>{entry.userAgent}</div>}</td><td className="whitespace-nowrap px-5 py-4 text-slate-500">{entry.ipAddress ?? 'Unknown'}</td><td className="max-w-sm px-5 py-4 text-slate-600 dark:text-slate-300">{entry.details ?? '—'}</td><td className="px-5 py-4">{entry.succeeded ? <span className="inline-flex items-center gap-1.5 rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-medium text-emerald-700 dark:bg-emerald-500/10 dark:text-emerald-300"><CheckCircle2 className="h-3.5 w-3.5" />Success</span> : <span className="inline-flex items-center gap-1.5 rounded-full bg-rose-50 px-2.5 py-1 text-xs font-medium text-rose-700 dark:bg-rose-500/10 dark:text-rose-300"><XCircle className="h-3.5 w-3.5" />Failed</span>}</td></tr>)}</tbody></table>{auditQuery.isLoading && <p className="p-8 text-center text-sm text-slate-500">Loading audit activity...</p>}{auditQuery.error && <p className="p-6 text-sm text-rose-600">Unable to load the audit log: {(auditQuery.error as Error).message}</p>}{!auditQuery.isLoading && !auditQuery.error && (auditQuery.data ?? []).length === 0 && <p className="p-8 text-center text-sm text-slate-500">No security activity recorded yet.</p>}</div></Card></div>;
}
